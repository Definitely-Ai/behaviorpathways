# Optional: base URL for metadata; falls back to Vercel's URL or localhost
NEXT_PUBLIC_SITE_URL=
# Optional: where contact form submissions are sent; defaults to site.contact.email
CONTACT_TO_EMAIL=
RESEND_API_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
HCAPTCHA_SITEKEY=
HCAPTCHA_SECRET=
PLAUSIBLE_DOMAIN=
